server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: khan
    password:
      1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect